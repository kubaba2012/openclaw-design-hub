<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- 颜色定义 -->
    <Color x:Key="PrimaryColor">#6366F1</Color>
    <Color x:Key="PrimaryDarkColor">#4F46E5</Color>
    <Color x:Key="AccentColor">#10B981</Color>
    <Color x:Key="BackgroundColor">#1E1E2E</Color>
    <Color x:Key="SurfaceColor">#2D2D3D</Color>
    <Color x:Key="SurfaceLightColor">#3D3D4D</Color>
    <Color x:Key="TextColor">#E4E4E7</Color>
    <Color x:Key="TextMutedColor">#A1A1AA</Color>
    <Color x:Key="UserBubbleColor">#6366F1</Color>
    <Color x:Key="AssistantBubbleColor">#3D3D4D</Color>

    <SolidColorBrush x:Key="PrimaryBrush" Color="{StaticResource PrimaryColor}"/>
    <SolidColorBrush x:Key="PrimaryDarkBrush" Color="{StaticResource PrimaryDarkColor}"/>
    <SolidColorBrush x:Key="AccentBrush" Color="{StaticResource AccentColor}"/>
    <SolidColorBrush x:Key="BackgroundBrush" Color="{StaticResource BackgroundColor}"/>
    <SolidColorBrush x:Key="SurfaceBrush" Color="{StaticResource SurfaceColor}"/>
    <SolidColorBrush x:Key="SurfaceLightBrush" Color="{StaticResource SurfaceLightColor}"/>
    <SolidColorBrush x:Key="TextBrush" Color="{StaticResource TextColor}"/>
    <SolidColorBrush x:Key="TextMutedBrush" Color="{StaticResource TextMutedColor}"/>
    <SolidColorBrush x:Key="UserBubbleBrush" Color="{StaticResource UserBubbleColor}"/>
    <SolidColorBrush x:Key="AssistantBubbleBrush" Color="{StaticResource AssistantBubbleColor}"/>

    <!-- 圆角 -->
    <CornerRadius x:Key="PillRadius">20</CornerRadius>
    <CornerRadius x:Key="PanelRadius">16</CornerRadius>
    <CornerRadius x:Key="BubbleRadius">12</CornerRadius>

    <!-- 字体 -->
    <FontFamily x:Key="MainFont">Microsoft YaHei UI, Segoe UI, Arial</FontFamily>

    <!-- 基础样式 -->
    <Style x:Key="BaseTextBlockStyle" TargetType="TextBlock">
        <Setter Property="FontFamily" Value="{StaticResource MainFont}"/>
        <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
        <Setter Property="TextWrapping" Value="Wrap"/>
    </Style>

    <!-- 按钮样式 -->
    <Style x:Key="PillButtonStyle" TargetType="Button">
        <Setter Property="Background" Value="{StaticResource SurfaceBrush}"/>
        <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border Background="{TemplateBinding Background}"
                            CornerRadius="{StaticResource PillRadius}"
                            Padding="{TemplateBinding Padding}">
                        <ContentPresenter HorizontalAlignment="Center"
                                          VerticalAlignment="Center"/>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="{StaticResource SurfaceLightBrush}"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- 输入框样式 -->
    <Style x:Key="ChatInputStyle" TargetType="TextBox">
        <Setter Property="Background" Value="{StaticResource SurfaceLightBrush}"/>
        <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Padding" Value="12,8"/>
        <Setter Property="FontFamily" Value="{StaticResource MainFont}"/>
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="AcceptsReturn" Value="True"/>
        <Setter Property="VerticalScrollBarVisibility" Value="Auto"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TextBox">
                    <Border Background="{TemplateBinding Background}"
                            CornerRadius="8"
                            Padding="{TemplateBinding Padding}">
                        <ScrollViewer x:Name="PART_ContentHost"
                                      VerticalAlignment="Center"/>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- 消息气泡样式 -->
    <Style x:Key="UserBubbleStyle" TargetType="Border">
        <Setter Property="Background" Value="{StaticResource UserBubbleBrush}"/>
        <Setter Property="CornerRadius" Value="{StaticResource BubbleRadius}"/>
        <Setter Property="Padding" Value="12,8"/>
        <Setter Property="MaxWidth" Value="400"/>
    </Style>

    <Style x:Key="AssistantBubbleStyle" TargetType="Border">
        <Setter Property="Background" Value="{StaticResource AssistantBubbleBrush}"/>
        <Setter Property="CornerRadius" Value="{StaticResource BubbleRadius}"/>
        <Setter Property="Padding" Value="12,8"/>
        <Setter Property="MaxWidth" Value="400"/>
    </Style>

</ResourceDictionary>